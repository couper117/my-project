/**
 * Static presentational data for the Hajj Services UI.
 *
 * Domain data (requirements, applications) comes from the backend via
 * `hajjApi`; the landing copy is admin-managed through the content CMS.
 */

/**
 * Hero backdrop, shared by the hajj landing, register and status pages — one
 * image per service, as marriage and good conduct already do. An aerial view of
 * the Kaaba at Masjid Al-Haram: the destination the whole service points at.
 */
export const HERO_IMAGE =
  'https://images.unsplash.com/photo-1591604129939-f1efa4d9f7fa?w=1600&q=80';

name: Migrate — Production

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type MIGRATE to confirm running migrations on production'
        required: true

jobs:
  migrate:
    name: Run migrations on production
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event.inputs.confirm == 'MIGRATE'
    environment:
      name: production
      url: https://isengesho.com
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: Build backend
        working-directory: apps/backend
        run: npm ci && npm run build

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.PRODUCTION_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.PRODUCTION_HOST }} >> ~/.ssh/known_hosts

      - name: Rsync compiled migrations to EC2
        run: |
          rsync -az \
            apps/backend/dist/database/migrations/ \
            ubuntu@${{ secrets.PRODUCTION_HOST }}:/opt/rmc-platform/apps/backend/dist/database/migrations/
          rsync -az \
            apps/backend/dist/config/ \
            ubuntu@${{ secrets.PRODUCTION_HOST }}:/opt/rmc-platform/apps/backend/dist/config/

      - name: Run migrations on EC2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PRODUCTION_HOST }}
          username: ubuntu
          key: ${{ secrets.PRODUCTION_SSH_KEY }}
          script: |
            set -e
            export NVM_DIR="$HOME/.nvm"
            source "$NVM_DIR/nvm.sh"

            echo "==> Running DB migrations..."
            cd /opt/rmc-platform
            ./node_modules/.bin/typeorm migration:run -d apps/backend/dist/config/data-source.js
            echo "==> Migrations complete"
